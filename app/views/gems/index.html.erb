<% provide(:title, "My Gems") %>
<h1>Dashboard</h1>

<% if @ruby_gems.empty? %>
  You haven't pushed any gems yet.
<% else %>
  <table>
    <thead>
      <tr>
        <th>Gem</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @ruby_gems.each do |g| %>
        <tr>
          <td><%= g.name %></td>
          <td><%= link_to 'Show', gem_path(g) %></td>
        </tr>
      <% end %>
  </tbody>
  </table>
<% end %>
